apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: sandeephegde-apps
  namespace: argocd
spec:
  description: Project for Sandeep Hegde's applications
  sourceRepos:
    - 'https://github.com/sandeep-ssh/terraform-aws-eks-argocd.git'
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
  namespaceResourceWhitelist:
    - group: '*'
      kind: '*'